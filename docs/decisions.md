# 决策

正式名OPL Med Cast；技术ID opl-medcast；领域medical_video；软件包独立版本。采用Framework标准声明式骨架，以OPL Book Forge为结构参考。将工作台十阶段归并为六个语义阶段、保留六个专业Skill，不建立新的媒体调度器。关键帧留制作工作区，通过catalog检索。OMA启动失败与本地开发验证分开报告，不伪造工程完成或资格。

正式发布复用 OPL Flow 的软件包工具与 Framework 的 publish-package.yml，以 OCI 不可变版本和 latest-stable 为准。不创建独立 ZIP 发布器；不创建或保留独立 GitHub Release 页面及附件。
