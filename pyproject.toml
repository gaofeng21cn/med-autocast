[project]
name = "opl-medcast-helpers"
version = "0.1.0"
description = "OPL Med Cast read-only workspace and delivery helpers"
requires-python = ">=3.10"
dependencies = ["PyYAML>=6,<7"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = []
